# 外测度

## 1. 定义
设 E 是 R^n 的子集，E 的勒贝格外测度定义为：
m*(E) = inf { sum(|I_k|) | E 包含于并 I_k，且 I_k 为欧氏空间中的开区间 }

## 2. 核心性质
1. 非负性：m*(E) >= 0，空集外测度为 0
2. 单调性：若 A 包含于 B，则 m*(A) <= m*(B)
3. 次可数可加性：m*(并 E_k) <= sum(m*(E_k))
4. 平移不变性：对任意向量 a，有 m*(E + a) = m*(E)
