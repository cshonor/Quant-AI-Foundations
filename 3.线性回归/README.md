# 第 3 章笔记：线性回归

## 本节核心

- 线性回归是一种常用的数值预测方法，用来估计连续值。  
- 典型应用：房价、股票价值、寿命、用户观看时长等。  
- 目标是找到一条“尽量贴近所有数据点”的直线。  

## 直观理解

- 图中散点代表观测数据，整体呈现近似线性趋势。  
- 我们希望画出一条线，让它与这些点的整体距离最小。  
- 这条线就是线性回归模型对数据规律的近似表达。  

## 类比（书中思路）

- 把散点看成城市中的房屋位置。  
- 画出的回归线像一条穿过城市的道路。  
- 道路越靠近大多数房屋，说明这条线越“符合需求”。  

## 一句话总结

> 线性回归就是用一条直线去近似数据中的变化趋势，并据此做连续值预测。

## 3.1 问题：预测房屋价格

### 场景

- 假设我们是房地产经纪人，需要给新房定价。  
- 房价会受很多特征影响：面积、房间数、位置、犯罪率、学校质量、到市中心距离等。  
- 目标是找到一个函数，用这些特征去预测新房价格。  

## 3.2 解决方案：建立房价回归模型

### 简化建模示例（单特征）

- 为了先理解核心思路，先只用一个特征：**房间数**。  
- 已知样本（房间数 -> 价格，单位万美元）：  
  - 1 -> 15  
  - 2 -> 20  
  - 3 -> 25  
  - 4 -> ?（待预测）  
  - 5 -> 35  
  - 6 -> 40  
  - 7 -> 45  

### 从样本中观察规律

- 每增加 1 个房间，价格大约增加 5 万美元。  
- 可理解为：房子有一个“基础价格”，再叠加“每个房间带来的增量价格”。  
- 这就是线性回归的核心：用线性关系描述特征与目标值之间的变化。  

### 一句话总结

> 在线性回归里，我们先从数据中找到“每增加一点特征，目标值大致变化多少”的规律，再用这个规律做预测。

## 3.3 核心概念：从公式出发

以模型公式 **房屋价格 = 10 + 5 × 房间数** 为例，下面是从线性回归角度对机器学习核心术语的定义：

| 概念 | 含义 | 本例 |
|------|------|------|
| **特征 (Feature)** | 用于预测的数据属性 | 房间数、犯罪率、房龄、面积等；本例中选 **房间数** |
| **标签 (Label)** | 要预测的目标 | **房屋价格** |
| **模型 (Model)** | 用特征预测标签的规则或公式 | 上述预测价格的公式 |
| **预测 (Prediction)** | 模型的输出 | 如"4 个房间的房约 30 万美元"，则预测值为 30 |
| **权重 (Weight)** | 公式中与各特征相乘的系数 | **5**（每个房间对应的价格增量） |
| **偏差 (Bias)** | 公式中不依附于特征的常数 | **10**（房屋的基础价格） |

### 下一步问题

> 这个公式是怎么得来的？更具体地说，如何让计算机自动得出合适的权重和偏差？

书中将用稍复杂的例子，结合第 2 章的「记忆 → 制定 → 预测」框架来回答：先记住其他房屋的价格，从中总结规律建立模型，再用该模型预测新房价格。

---

## 3.4 记忆步骤：查看现有房屋的价格

### 问题与思路

> 我们是如何想出这个公式的？更具体地说，如何让计算机自动得出权重和偏差？

用第 2 章的「**记忆 → 制定 → 预测**」框架来回答：先记住其他房屋的价格，为该价格制定一个模型，再用该模型预测新房价格。

### 表 3.2：稍复杂一点的房屋数据集

| 房间数量/个 | 价格/万美元 |
|------------|------------|
| 1 | 15.5 |
| 2 | 19.7 |
| 3 | 24.4 |
| 4 | **?**（待预测） |
| 5 | 35.6 |
| 6 | 40.7 |
| 7 | 44.8 |

---

## 3.5 数据可视化：图 3.3

### 数据特点

- 此数据集与前一个（表 3.1）类似，但价格**不严格**遵循「每间房多 5 万」的规律。
- 与原始数据集相差不大，仍可假设类似模式可用于预测。
- **重要习惯**：拿到新数据集时，第一步往往是**画图**。

### 图 3.3 含义

- 横轴：房间数量  
- 纵轴：房屋价格（万美元）  
- 散点整体呈上升趋势，适合用线性模型近似，但并非完美直线。

---

## 3.6 制定步骤：制定预估房屋价格的规则

### 精炼公式

表 3.2 与表 3.1 非常接近，可沿用类似公式，但需考虑实际价格与预测的差异：

房屋价格=10+5×房间数+小误差

- 预测虽不完全准确，但预期非常接近真实值。
- 核心问题：**我们/计算机是如何得出这个公式的？**

### 几何意义

公式 y=10+5x（y 为价格，x 为房间数）在坐标系中是一条直线：

- **斜率 (slope)**：5 → 对应 **权重 (weight)**，即每增加 1 个房间，价格增加约 5 万美元。
- **y 轴截距 (y-intercept)**：10 → 对应 **偏差 (bias)**，即房间数为 0 时的基础价格。

---

## 3.7 斜率和 y 轴截距（数学基础）

### 斜率

- **定义**：衡量直线陡峭程度，等于「竖直方向上升量 ÷ 水平方向移动量」，整条线上比值恒定。
- **在 ML 中**：斜率对应特征的**权重**，表示特征每增加 1 单位，标签预期变化多少。
- **特例**：水平线斜率为 0；向下倾斜时斜率为负。

### y 轴截距

- **定义**：直线与纵轴（y 轴）的交点高度。
- **在 ML 中**：y 轴截距对应**偏差 (bias)**，表示所有特征为 0 时的预测值。

### 线性方程

- **形式**：y = mx + b（m 为斜率，b 为 y 轴截距）。
- **在 ML 中**：x 为特征值，y 为预测值；m 为权重，b 为偏差。

### 房价公式示例

- 斜率 5：每多 1 个房间，估计价格增加 **5 万美元**。
- 截距 10：假设房间数为 0 时，基础价格为 **10 万美元**。

---

## 3.8 最优拟合线：图 3.4

### 图 3.4 内容

- 散点代表各房屋（房间数 vs 价格）。
- 直线：**价格 = 10 + 5 × 房间数量**，即线性回归模型。

### 为什么选这条线？

在所有可能的直线中（每条都有自己的公式），我们选择**最接近所有点**的那条。可能有更贴合的线，但至少这条线合理，不会远离数据点。

回到「房屋与道路」的类比：有一些房屋，想画一条**尽可能靠近所有房屋的道路**。

### 如何找到这条线？

具体方法留待后续讨论。可先想象：水晶球里有很多点，需要找一条靠近所有点的直线——这正是线性回归要解决的问题。

---

## 3.9 如果有更多变量怎么办？多元线性回归

单特征模型（如只用房间数）可推广为**多元线性回归**：加入面积、学校质量、房龄等变量，每个特征乘以其权重后相加，再加上偏差，得到预测价格。

### 示例公式

价格=3×房间数+0.15×面积+1×教学质量−2×房龄+5

### 权重的含义

| 权重类型 | 含义 | 本例 |
|----------|------|------|
| **正权重** | 特征与价格正相关 | 房间数(3)、面积(0.15)、教学质量(1)：越大/越好，预期价格越高 |
| **负权重** | 特征与价格负相关 | 房龄(-2)：越老越便宜 |
| **接近 0** | 特征与价格几乎无关 | 若某特征（如「邻居姓氏以 A 开头的人数」）无关，合理模型中其权重为 0 或接近 0 |
| **绝对值大** | 模型认为该特征很重要 | 单特征时房间数权重大，说明对价格影响大 |

---

## 3.10 高维视角：从直线到平面再到超平面

### 维度和列数（回顾第 2 章）

- 数据集的**列数**对应其**维度**。
- 两列 → 平面上的点（2D）；三列 → 三维空间中的点（3D）。

### 三变量时的线性回归

- 三个特征时，线性回归不再是「一条线」，而是**一个平面**，且要尽可能靠近所有数据点。
- 即**三变量多元线性回归**。

### 类比：苍蝇与纸板

> 想象房间里有很多苍蝇悬停在固定位置，我们的任务是把一块大纸板放在空中，**尽可能靠近所有苍蝇**。

纸板 = 拟合平面，苍蝇 = 数据点。

### 更高维与本章重点

- 特征很多（列数 > 3）时，无法直接画图，但仍可抽象为「**多变量线性公式**」（高维空间中的超平面）。
- **本章**主要讲**单特征**线性回归的建模与训练。
- 多特征时**过程类似**：建议边学边联想——每个概念如何推广到多特征情形。

---

## 3.11 出现的一些问题和快速答案

### (1) 如果模型出错会怎样？

模型用来**估计**房价，几乎总是会有**小误差**，很难做到完全精确。训练的目标就是：找到在给定数据点上**误差最小**的模型（即优化损失函数）。

### (2) 如何想出预测公式？如果有几千个房屋呢？

这是**本章要解决的核心问题**。六七个房屋时，可以目测画一条接近它们的线；若有**几千个**房屋，就难以靠手画完成。本章将设计算法/程序，让**计算机**自动找到合适的拟合线。

### (3) 新房屋上市后，能否用新信息更新模型？

**完全可以！** 模型会设计成便于在出现新数据时更新。若每次新数据到来都要重新计算整个模型，实用性会大打折扣——**易于增量更新**是机器学习中的持续追求。
